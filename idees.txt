parsing: diviser les fichiers texte. le mot devant les ":" est juste le type de hub.
apres, il faut split en <nom> <x> <y> [metadata]

quand le mot cle devant est "connection", il faut juste le lire <nom1>-<nom2> apres les ":"

donc:

split par ligne, chaque ligne, split pour voir le mot cle et agir en consequence 

cree classe de type Hub (avec potentiellement sous-classe start-hub et end-hub)
cree classe pour hub_metadata et pour connection_metadata
cree classe pour connection qui prend 2 hubs en argument

if split[1] == start_hub || split[1] == hub || split[1] == end_hub:
	split[2] = hub1.name
	split[3] = hub1.x
	split[4] = hub1.y
	split[5:] = hub1.metadata # tout ce qui est mis entre [] est metadata

elif split[1] == connection:
	split[2] == connection.hub1
	split[3] == connection.hub2
	split[4] == connection.metadata

faire condition pour metadata (hub et connection)

with open("maps.easy.01_linear_path.txt") as f:
    for line in f:
        if line.split(0) == start_hub:
			start_hub = Start_hub(line.split(1), line.split(2), line.split(3), line.split(4), line.split(5), line.split(6))
